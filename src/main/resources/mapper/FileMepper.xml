<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.fileupload.file.FileMapper">

    <insert id="excelUpload" parameterType="java.util.List">
        INSERT INTO tbl_excelfile
        (phone_num, name, email)
        VALUES
        <foreach collection="list" item="item">
            (
                #{item.phoneNum},
                #{item.name},
                #{item.email}
            )
        </foreach>
    </insert>

    <insert id="excelDataUpload">
        INSERT INTO tbl_excelfiledata
        (file_name, file_type, file_created_at, file_processed_at, operation_status, consequence)
        VALUES(#{fileName}, #{fileType}, #{fileCreatedAt}, #{fileProcessedAt}, #{operationStatus}, #{consequence});

    </insert>

    <select id="pkKeyCheck" parameterType="FileVO" resultType="string">
        SELECT name
        FROM tbl_excelfile
        WHERE phone_num = #{phoneNum}
    </select>

    <select id="showUser" resultType="FileVO">
        SELECT *
        FROM tbl_excelfile
    </select>

    <select id="findForExcelData" resultType="FileDataVO">
        SELECT *
        FROM tbl_excelfiledata
        WHERE operation_status = 'waiting' OR operation_status = 'failed'
    </select>

    <insert id="createdTempFile" parameterType="java.util.List">
        INSERT INTO tbl_tempfile
        (temp_file_name, temp_file_status)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.tempFileName},
            #{item.tempFileStatus}
            )
        </foreach>
    </insert>

    <select id="selectedTempFile" resultType="string">
        SELECT temp_file_name
        FROM tbl_tempfile
    </select>

    <update id="excelDataConectedTemp">
        UPDATE tbl_excelfiledata
        SET temp_file_name = #{tempFileName}
        WHERE file_no = #{tempFileno}
    </update>
</mapper>
