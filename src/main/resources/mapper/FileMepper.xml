<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.fileupload.file.FileMapper">

    <insert id="excelUpload" parameterType="java.util.List">
        INSERT INTO tbl_excelfile
        (excelfile_phone_num, excelfile_name, excelfile_email)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.excelfilePhoneNum},
                #{item.excelfileName},
                #{item.excelfileEmail}
            )
        </foreach>
    </insert>

    <insert id="excelDataUpload">
        INSERT INTO tbl_excelfiledata
        (excelfiledata_name, excelfiledata_type, excelfiledata_upload_time, excelfiledata_processing_time, excelfiledata_operation_status, excelfiledata_result)
        VALUES(#{excelfiledatName}, #{excelfiledataType}, #{excelfiledataUploadTime}, #{excelfiledataProcessingTime}, #{excelfiledataOperationStatus}, #{excelfiledataResult});

    </insert>

    <select id="pkKeyCheck" parameterType="java.util.List" resultType="string">
        SELECT excelfile_name
        FROM tbl_excelfile
        WHERE
        <trim prefix="(" suffix=")" prefixOverrides="OR">
            <foreach collection="list" item="item">
                <trim prefix="OR">
                    excelfile_phone_num = #{item.excelfilePhoneNum}
                </trim>
            </foreach>
        </trim>
    </select>

</mapper>